
  <!-- <li><%= link_to book.id, book_path(book) %></li> -->
  <!-- <li><%= book.parent_id %></li> -->
  <li class="list-group-item"><%= book.title %>
    <span class="badge badge-primary badge-pill"><%= book.children.count %></span>
  </li>

  <!-- <li><%= book.info %></li> -->
  <!-- <li><%=l book.created_at %></li> -->
  <li class="list-group-item">
    <%= link_to t('.show', :default => t("helpers.links.show")),
                admin_book_path(book), :class => 'btn btn-default btn-xs' %>
    <%= link_to t('.edit', :default => t("helpers.links.edit")),
                edit_admin_book_path(book), :class => 'btn btn-default btn-xs' %>
    <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                admin_book_path(book),
                :method => :delete,
                :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                :class => 'btn btn-xs btn-danger' %>
  </li>
