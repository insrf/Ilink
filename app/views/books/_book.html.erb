
  <!-- <li><%= link_to book.id, book_path(book) %></li> -->
  <!-- <li><%= book.parent_id %></li> -->
  <li><%= book.title %>
    <span class="badge badge-primary badge-pill"><%= book.children.count %></span>
  </li>

  <!-- <li><%= book.info %></li> -->
  <!-- <li><%=l book.created_at %></li> -->
  <li>
    <%= link_to t('.rent_book', :default => t("helpers.links.rent")),
                new_book_rent_book_path(book), :class => 'btn btn-default btn-xs' %>
    <%= link_to t('.show', :default => t("helpers.links.show")),
                book_path(book), :class => 'btn btn-default btn-xs' %>
    <%= link_to t('.edit', :default => t("helpers.links.edit")),
                edit_book_path(book), :class => 'btn btn-default btn-xs' %>
    <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                book_path(book),
                :method => :delete,
                :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                :class => 'btn btn-xs btn-danger' %>
  </li>
